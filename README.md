# Restaurant Management System 🍽️

A C++ console application designed to manage restaurant operations efficiently. This project demonstrates file handling, object-oriented programming, and data management using CSV files.

## 📂 Project Structure
- **main.cpp**: The core application logic.
- **Data Files (CSV)**:
  - `food.csv`: Menu items and prices.
  - `staff.csv`: Employee records.
  - `customers.csv`: Customer database.
  - `orders.csv`: Order history and tracking.

## 🚀 Features
- Add, update, and delete menu items.
- Manage staff information.
- Process customer orders using file I/O operations.
- Data persistence using CSV format (no external database required).

## 🛠️ Tech Stack
- **Language**: C++
- **Storage**: CSV Text Files

## 💻 How to Run
1. Clone the repository.
2. Compile `RestaurantSystem.cpp` using any C++ compiler (e.g., g++).
3. Ensure all `.csv` files are in the same directory as the executable.
4. Run the application.
